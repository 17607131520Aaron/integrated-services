# 开发环境配置
spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/integrated_services_dev?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8
      username: root
      password: 123456

  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 0

# 日志级别
logging:
  level:
    root: info
    com.enterprise.integrated: debug
    org.springframework.web: debug
    com.alibaba.druid: debug
